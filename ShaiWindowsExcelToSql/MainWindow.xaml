<Window x:Class="ShaiWindowsExcelToSql.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShaiWindowsExcelToSql"
        xmlns:vm="clr-namespace:ShaiWindowsExcelToSql.ViewModels"
        mc:Ignorable="d"
        Title="ShaiExcelToSql - Windows" Height="700" Width="1000">
    
    <Window.Resources>
        <!-- Simple Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Connection -->
            <RowDefinition Height="Auto"/> <!-- Query Input -->
            <RowDefinition Height="*"/>    <!-- Results -->
            <RowDefinition Height="Auto"/> <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Connection Section -->
        <GroupBox Header="Connection" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Connection String:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding ConnectionString, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0" Padding="2"/>
            </Grid>
        </GroupBox>

        <!-- Query Section -->
        <GroupBox Header="SQL Query" Grid.Row="1" Margin="0,0,0,10" Height="150">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox Text="{Binding SqlQuery, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Padding="5"/>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                    <Button Content="Execute Query" Command="{Binding ExecuteQueryCommand}" Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="Export to Excel" Command="{Binding ExportToExcelCommand}" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Results Section -->
        <GroupBox Header="Results" Grid.Row="2" Margin="0,0,0,10">
             <Grid>
                 <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>
                 </Grid.RowDefinitions>
                 
                 <!-- Tabs for multiple result sets if needed -->
                 <ComboBox Grid.Row="0" 
                           ItemsSource="{Binding ResultSets}" 
                           SelectedItem="{Binding SelectedResultSet}" 
                           DisplayMemberPath="SheetName"
                           Visibility="{Binding HasMultipleResultSets, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Margin="0,0,0,5" Width="200" HorizontalAlignment="Left"/>

                <!-- Use ItemsSource directly binding to DataTable's DefaultView is easiest for WPF DataGrid -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding QueryResults}" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True"
                          CanUserAddRows="False"/>
             </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="100" Height="15" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
